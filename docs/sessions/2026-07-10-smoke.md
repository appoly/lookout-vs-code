# Smoke test results — 2026-07-10

Plan: [docs/TESTPLAN.txt](../TESTPLAN.txt). One row per numbered item: mark **pass**, **fail**, or **skip**, and add a note for anything that isn't a clean pass. File defects found here in the "Defects raised" section at the bottom.

## Environment

- Commit under test:
- Install path (F5 EDH / installed VSIX / both):
- VS Code version + OS (desktop / WSL remote):
- Codex CLI version:
- Claude Code version:
- Tester:

## Results

### A. Rename integrity

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 1 | Parful icon + three views render | Pass | |
| 2 | Commands only under Parful category | Pass | |
| 3 | All 20 settings found under "parful" | Pass | |
| 4 | Only PARFUL_* env vars in agent terminal | Pass | |

### B. Launch and layout

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 5 | Picker + column-two placement, column one free | Pass | |
| 6 | Terminal names / tree row descriptions | Pass | |
| 7 | Split lands beside parent | Fail | Split landed in bottom "Terminal" pane, parent was in top-right of window |
| 8 | Panel location setting honored | Pass | |
| 9 | Missing executable failure mode observed | Pass | Didn't show up in Problems |

### C. Lifecycle, attention, bell

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 10 | Claude working / permission / waiting / unread | Pass | |
| 11 | Codex /hooks trust flow + notify fallback | Pass | |
| 12 | Delegated agents: background vs waiting precedence | Pass | |
| 13 | Bell plays; volume 0 + speaker toggle mute; WSL fallback | Fail |tested on WSL only |
| 14 | Custom agent hook command + stdin JSON | Not-tested | |
| 15 | Exit tracking + notifyOnAgentExit | Pass | The app is not producing any notifications, aside from what shows in "Agents", maybe this is correct. |

### D. Navigation

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 16 | Focus, quick-pick, ctrl+alt+n, cycling, rename | Pass | I did see an accurate notification here in the bottom right of VSC, which makes me think maybe I've missed the others. |

### E. Review view

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 17 | Modify/add/delete/rename diffs against baseline | Pass | Not tested thoroughly |
| 18 | Agent-first group labels, branch + count description | Half-pass | Works for workspace changes but not for Plans & Docs |
| 19 | Branch-switch warning + stale baseline wording | Pass | Not possible to resolve the warning, likely will be a legit branch change |
| 20 | Linked-worktree discovery outside workspace | Fail | I spawned in agent in a separate repo with a standard terminal command (on the same level `../vismo`) which created a new worktree. At no point was this agent discovered. It should be discovered when it is created, regardless of if/when it is using a worktree. |
| 21 | Artifact globs classified out of Workspace Changes | Half-pass | Largely works, however not categorised by agent and docs/TESTPLAN.txt showed as workspace changes rather than plans & docs, and I'm not entirely sure why as docs/DECISIONS.md showed in plans & docs. |
| 22 | Recent Images opt-in + image editor | Pass | |
| 23 | Diagnostics / Tasks / SCM / Open Browser fallback | Pass | |

### F. Usage limits

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 24 | Codex numbers verified against provider UI; Spark opt-in | Pass | |
| 25 | Claude 5h/7d verified; API-key never 0% | Pass | |
| 26 | Status bar summary, thresholds, staleness | Pass | |

### G. Persistence and restore

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 27 | Reload reattach; honest bridge state | Pass | |
| 28 | Restored custom session refusals | Skipped | |
| 29 | Closed state + sidebar removal survives reload | Failed | Closed terminal manually, showed as "Claude is working".  |

### H. Trust

| # | Item | Result | Notes |
| --- | --- | --- | --- |
| 30 | Untrusted: read-only review/usage, launches blocked | Pass | |

## Known-fail (expected, not a finding)

- Restart Agent Command on a *live* agent types into its stdin — ROADMAP item 8, fix pending. Restart was tested on exited sessions only: ☐

## Exit criteria (release gates)

- ☐ launch → monitor → jump → review loop passes interactively
- ☐ no provider limit shown as zero when unknown
- ☐ restored sessions never silently claim a working bridge
- ☐ no auth file or terminal output read
- ☐ no shared-worktree misattribution
- ☐ package, CI, and this matrix green

## Defects raised

- Plans need to be categorised with regards to the agent that created them, same as code changes.
- Can't hear agent completed notification bell, can't find a volume slider, mute icon doesn't change when toggled.
- Not sure if Problems is working - started a nonexistent command which errored and the agent reflected this, but didn't show up in Problems.
- I can now select codex vs. claude vs. custom from the add agent flow, but I can't select its starting folder, important as may have agents working on multiple apps at once, want to manage them from the same window.
- docs/TESTPLAN.txt showed as workspace changes rather than plans & docs, and I'm not entirely sure why as docs/DECISIONS.md showed in plans & docs.
- Plans & Docs needs investigating generally, it's also showing docs which were previously created and are unchanged, we only want to see docs created by currently running agents so we can review/approve.

## Observations

- I actually don't mind if Parful spawns agents in the VS Code terminal window, so long as it acts as a robust and effective tool for managing them.
- Parful should also discover agents which were not created by itself, if possible - investigate.
- Agents should have a bin icon next to split.

## Verdict

<!-- overall: ready to proceed to Phase 1 / blocked on ... -->
